import {AppBar, Box} from "@mui/material";

export const Header = (props) => {
    return (
        <Box sx={{ flexGrow: 1 }}>
            <AppBar position="static">

            </AppBar>
        </Box>
    );
}